cilium (0.9.90) UNRELEASED; urgency=medium

  [ root ]
  * Initial release. (Closes: #XXXXXX)

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ André Martins ]
  * cni: ommiting errors for endpoint not found on DEL
  * cni: setting net.ipv6.conf.all.disable_ipv6=0

  [ Thomas Graf ]
  * k8s: Use existing `10-cilium.conf` CNI configuration if it exists
  * proxy: Create access log file if it does not exist

  [ John Fastabend ]
  * cilium: include versions in clang/kernel version error report

  [ peiqi ]
  * renew cilium policy setup script in examples

  [ John Fastabend ]
  * cilium: push version info into debug logs of epbf join
  * cilium: data

  [ Eloy Coto ]
  * Monitor: omiting panic when no events Fix #757

  [ Jarno Rajahalme ]
  * tests: Show which test is running.
  * bpf: Add probe for LPM trie map type.
  * bpf: More rigorous bpf map probes.
  * policy: Add L3 rule validation and unit tests.
  * bpf: Support L3/IP access policy.
  * pkg/maps: Add support for flags in map creation.
  * pkg/maps: Add new cidrmap.
  * pkg/policy: Add L3 support.

  [ André Martins ]
  * policy: fix index out of range for MatchExpressions

  [ Alexander Alemayhu ]
  * tests: make link check ready for vagrant
  * Vagrantfile: add dependencies for building the docs
  * Documentation: add bpf updates 05

  [ John Fastabend ]
  * bpf: multiple ports in the policy causes compilation failure
  * cilium: merge ingress and egress proxy tests
  * cilium: add tests for with multiple l4 port maps
  * cilium: bindata

  [ Eloy Coto ]
  * BPF init: Check that cilium is present on the path fix #742 #743

  [ Steven Ceuppens ]
  * cilium: Move common/config.go to pkg/option/config.go - fix #760

  [ Thomas Graf ]
  * Update AUTHORS
  * Documentation: Provide installation instructions for key-value store
  * labels: Support regular expressions in label filters

  [ Sergey Generalov ]
  * New Cilium diagrams for the main README

  [ Steven Ceuppens ]
  * cilium: Change Consumable LabelList to LabelArray - fix #838

  [ Thomas Graf ]
  * k8s: Move pkg/k8s/types into pkg/k8s
  * labels: Move label source definition into labels package
  * labels: Rework of label source
  * Documentation: Provide documentation on labels

  [ Alexander Alemayhu ]
  * Makefile: add test target for applying changes
  * Vagrantfile: install go-bindata (Closes: #859)
  * cilium: fix outdated bindata

  [ Thomas Graf ]
  * Documentation: Add option to edit /etc/fstab

  [ Ian Vernon ]
  * tests: add K8s getting-started-guide test

  [ Michi Mutsuzaki ]
  * Fix L7 policy in the getting started guide

  [ Thomas Graf ]
  * Documentation: Document behaviour for existing pods
  * Documentation: Document minimal version of kubectl for ThirdPartyResources

  [ Jon Davies ]
  * cilium-ds.yaml: Added health checks to consul pods.

  [ André Martins ]
  * tests: add no-pager flag to journalctl

  [ Thomas Graf ]
  * k8s: Ignore headless services

  [ André Martins ]
  * plugins: fix add endpoint panic for len IDs less 5

  [ Thomas Graf ]
  * README refresher

  [ Madhu Challa ]
  * bpf: Encapsulation support for standalone loadbalancer

  [ Thomas Graf ]
  * bpf: Disable rp_filter on cilium_host
  * bpf: Update bindata
  * Documentation: Improve CNI configuration section

  [ Ian Vernon ]
  * daemon: update policy enforcement w/ no endpoints

  [ Thomas Graf ]
  * Force doc rebuild
  * Revert "bpf: Update bindata"
  * Revert "bpf: Disable rp_filter on cilium_host"
  * Revert "bpf: Encapsulation support for standalone loadbalancer"

  [ Ian Vernon ]
  * daemon: fix log message for kvstore opt

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding etcd version packages
  * Revert "etcd: checking for etcd version"
  * Revert "Godeps: adding etcd version packages"

  [ Tobias Klauser ]
  * Documentation: update cilium version to 0.9.0 in admin guide

  [ Alexander Alemayhu ]
  * Vagrantfile: install pip by default
  * Revert "tests: make link check ready for vagrant"

  [ Thomas Graf ]
  * bpf: Update bindata.go with go-bindata + go-1.8.3
  * tests: Disable go 1.8.3 specific unit test for now

  [ shijunqian ]
  * Documentation: fix clerical error

  [ Eloy Coto ]
  * CMD - service list command refactoring Fix #744

  [ André Martins ]
  * forcely setting UTC for Time.Now()

  [ Alexander Alemayhu ]
  * bpf/Makefile: add target for analyzing files

  [ Ian Vernon ]
  * tests/k8s/Vagrantfile: reset kubeadm before init
  * Vagrantfile: use different intnet per build

  [ Alexander Alemayhu ]
  * tests: add separate script for pulling in docker images

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding go-version from hashicorp

  [ Thomas Graf ]
  * proxy: Extended logging of requests
  * k8s: Add unit test for default deny rule

  [ Alexander Alemayhu ]
  * daemon: use endpoint specific error in endpoint TriggerPolicyUpdates
    (Closes: #677)

  [ Thomas Graf ]
  * vendor: Update vendor/github.com/vishvananda/netlink/

  [ Ian Vernon ]
  * daemon: default to empty string for key-value store

  [ Daniel Borkmann ]
  * cilium: add CiliumIPv6 StringNoZeroComp handler

  [ Thomas Graf ]
  * proxy: Log record format improvements
  * Documentation: Remove demo_docs as they trigger doc build errors
  * Documentation: Fix broken reference
  * kvstore: Move all code to pkg/kvstore and add singleton client

  [ André Martins ]
  * examples: update kubernetes example

  [ Alexander Alemayhu ]
  * Documentation: add blank lines after listing item

  [ Thomas Graf ]
  * endpoint: Factor our endpoint management into pkg/endpoint
  * nodeaddress: Move node address code into own package
  * proxy: Provide source and destination endpoint information as available

  [ Alexander Alemayhu ]
  * tests: reduce sleep calls

  [ Jonathan Davies ]
  * cilium-ds.yaml: Added healthchecks for cilium pods.

  [ Thomas Graf ]
  * AUTHORS: Add recent new authors

  [ Alexander Alemayhu ]
  * Documentation: minor nitpicks and rewording
  * Documentation: use git to set version info

  [ André Martins ]
  * kvstore: fix race conditions in GetWatchers

  [ Thomas Graf ]
  * proxy: Provide correct source endpoint ID
  * daemon: When BPF generation fails, log each output line separate warning
  * logging: Disable formatter to have simple format in TTY
  * daemon: Turn unnecessary info messages into debugging messages

  [ Tobias Klauser ]
  * daemon: remove unnecessary cgo integration

  [ Thomas Graf ]
  * proxy: Fix race when looking up endpoint metadata

  [ John Fastabend ]
  * cilium: proxy uses incorrect daddr when services are used
  * cilium: bindata

  [ Tobias Klauser ]
  * pkg/bpf: remove unnecessary cgo integration
  * pkg/bpf: add wrapper constants for bpf map types and flags

  [ Alexander Alemayhu ]
  * daemon: remove bindata from git
  * contrib: add script for checking bindata
  * daemon: track go version used for bindata updates
  * daemon: only use tracked files in go-bindata
  * Dockerfile: make adjustments now that bindata.go is gone

  [ André Martins ]
  * kvstore: erroring out with the default kvstore IPs
  * retrieving K8s PodCIDR from k8s-apiserver

  [ Ian Vernon ]
  * tests: gather /var/run/cilium, /var/lib/cilium as part of Jenkins builds

  [ Ross Guarino ]
  * docs(architecture): use /64 cluster prefix

  [ Amre Shakimov ]
  * add support for CNI 0.2.x spec

  [ André Martins ]
  * packaging: add .git to the list of files to copy
  * packaging: add scripts directory to the list of files to copy
  * tests: changed docker build to make docker-image
  * Vagrantfiles: excluding packaging docker/stage for rsync
  * Moved make clean from docker/Makefile to Dockerfile

  [ John Fastabend ]
  * cilium: add test for lb + proxy

  [ Thomas Graf ]
  * bindata: Remove auto commit on apply
  * bindata: Store SHA and version in daemon/bpf.sha

  [ André Martins ]
  * add bindata.go to the list of ignore files
  * daemon: reoder flags and enhanced flags description

  [ craigbox ]
  * Update gettingstarted.rst for minikube 0.20

  [ Thomas Graf ]
  * bpf: Only create conntrack entries for TCP SYN packets

  [ André Martins ]
  * tests: add multinode k8s tests
  * Jenkins: enabled k8s multi-node tests on jenkins
  * tests: changing VMs conflicting IPs

  [ Tobias Klauser ]
  * Fix broken links to Kubernetes docs
  * pkg/bpf: get and store owner program type for maps

  [ Amre Shakimov ]
  * fix include in Makefile

  [ André Martins ]
  * daemon: returning clearer k8s error messages
  * Godeps: updating etcd client
  * etcd: make necessary changes for etcd 3.2.1

  [ Amre Shakimov ]
  * use \$(RM) instead of rm for cleaning

  [ Dan Wendlandt ]
  * Fix K8s typo in main README

  [ André Martins ]
  * k8s: removing verbose debug message

  [ Tobias Klauser ]
  * pkg/bpf: explicitly include stdint.h

  [ André Martins ]
  * tests: restoring bash trace in helpers

  [ Thomas Graf ]
  * tests: Wait for endpoints to become available before invoking netperf
  * tests: copy_files should never fail
  * k8s-tests: Use files of local git tree

  [ Alexander Alemayhu ]
  * cmd: add description for cilium command (Closes: #1049)

  [ Thomas Graf ]
  * tests/k8s: Make k8s-gsg-test.sh error out on failure
  * tests/k8s: Fix path to Kubernetes example files
  * tests/k8s: Increase memory to 2GB
  * tests/k8s: Use existing helper scripts to wait for state changes
  * Vagrantfile: Do not inherit DNS server of host
  * kubernetes: Add scheduler tolerations to schedule Cilium on NotReady nodes

  [ Alexander Alemayhu ]
  * cmd, model: add verbose field to IdentityContext (Closes: #693)

  [ Tobias Klauser ]
  * pkg/bpf: use ClockGettime from x/sys/unix instead of C wrapper

  [ André Martins ]
  * Vagrantfile: use build IDs as part of VMs names

  [ Thomas Graf ]
  * nodeaddress: Retrieve hostname if available
  * tests: Move documentation checks to the end
  * Dockerfile: Include bpf-map tool in container image
  * Vagrantfile: Include bpf-map tool in developer vagrant box

  [ André Martins ]
  * ct: add aliend_id in ct maps
  * CT: cleaning entries when new policy denies them

  [ Amre Shakimov ]
  * tools: fix Makefile for docker

  [ Thomas Graf ]
  * node: Split EndpointsCIDR into IPv4 and IPv6 field
  * bpf: Add NonPersistent flag to force removal of map on startup
  * bpf: Simplified encapsulation mode
  * cli: Command to print contents of local tunnel endpoint map
  * daemon: Use real node address of host
  * node: Populate local node in tunnel endpoint map
  * lxcmap: Support migration of map format
  * agent: Add --ipv6-range to set per node IPv6 allocation prefix
  * vagrant: Use --ipv6-range to set per node prefix
  * agent: Print IPv4/IPv6 node IPs when starting up
  * agent: Provide options to overwrite IPv4/IPv6 node address
  * agent: Derive real IPv6 node adress and split router address
  * tests/lb: Do not rely on cilium_host having a global scope addres
  * tests/proxy: Show return http codes
  * agent: Masquerade IPv4 packets which leave the host
  * bpf: Delay opening of maps to after mount
  * tests/k8s: Adjust k8s multi node to new multi node mode
  * agent: Add --ipv4-cluster-cidr-mask-size option
  * nodeaddress: Validate that cluster prefix is great then node alloc prefix
  * cni: Install cni loopback driver if not installed already

  [ Ian Vernon ]
  * tests: pick up k8s test logs, other enhancements

  [ Michi Mutsuzaki ]
  * Refactor k8s.Client

  [ André Martins ]
  * k8s: change daemon's k8s clientSet to Interface
  * k8s: add node IP CIDR in the annotations node

  [ Daniel Dao ]
  * move from godeps to https://github.com/LK4D4/vndr

  [ Thomas Graf ]
  * k8s: Correctly implement ClusterIP for type NodePort and LoadBalancer

  [ Daniel Dao ]
  * vendor.conf: update etcd to 3.2.1
  * remove unused dependencies detected by vndr
  * remove Godeps manifest since we moved to vndr

  [ Thomas Graf ]
  * examples/demo: Clean up star wars demo

  [ Alexander Alemayhu ]
  * tests: check printed container ids
  * pkg: introduce new package for handling byteorder
  * bpf: don't call bpf_htonl for LXC_IPV4
  * byteorder: add documentation file for byteorder package (Closes: #285, 16)

  [ Tobias Klauser ]
  * pkg/bpf: add new sock ops prog type

  [ André Martins ]
  * K8s: keeping namespace label set in cilium NP
  * tests: adding l7 stresstest on different namespaces
  * tests: ignoring .json files in 00-no-sleep.sh
  * Jenkinsfile: add timestamps to builds
  * endpointmanager: fixed remove locked for DockerEndpointID

  [ Eloy Coto ]
  * [BPF/probes]: Added kernel checks Fixes: #891

  [ André Martins ]
  * k8s: implemented CiliumNetworkPolicy multi rule
  * tests: add one more example to tests multi CNP enforcement
  * tests: adding nodeselector for bookinfo spec

  [ Ian Vernon ]
  * Documentation: add CoreOS getting-started-guide

  [ Thomas Graf ]
  * tests: Disable CT cleanup tests for now
  * policy: Introduce policy repository revision
  * api: Return policy revision via API
  * api: Regenerate API code
  * agent/client: Report policy revision via API and print in CLI
  * agent: Report repository revision as endpoint policy build
  * cilium: New 'cilium policy wait <revison>` command
  * tests: Add helpers to wait for all endpoints to run a policy revision
  * Makefile: run etcd 3.1.0 for unit tests
  * vagrant: Fail if cilium.service fails and print log

  [ Thomas Bachman ]
  * Fix typo in demo1.sh

  [ Thomas Graf ]
  * find-llvm.sh: Add fast track for common location
  * bpf: Support encapsulation for packets received from a netdev
  * daemon: Add options to specify service CIDR
  * daemon: Point cluster CIDR to cilium_host in encapsulation mode
  * daemon: Automatically reserve conflicting local IPv4 routes in alloc prefix
  * proxy: Provide original identity as SO_MARK
  * bpf: Add additional debugging for bpf_netdev
  * bpf: Compile bpf_host.o after ENCAP_IFINDEX has been derived
  * agent: Masquerade traffic from host into cilium cluster
  * bpf: Support reaching the local node from the overlay
  * bpf: Store source identity in proxy map
  * proxy: Inherit endpoint source identity from proxy map
  * bpf: Attach bpf_host.o at egress of cilium_host
  * tests: Fix 06-lb.sh
  * bpf: Redirect to HOST_IFINDEX when doing proxy redirect
  * tests: Remove multinode L7 tests workaround

  [ Ian Vernon ]
  * cmd: add output options for cilium monitor

  [ Thomas Graf ]
  * bpf: Provide identity in capture messages
  * bpfdebug: Improve DumpInfo() output
  * monitor: Decode layers unless disabled
  * monitor: Print separator between messages in non-info mode
  * bpf: Do not require DEBUG flag to emit basic tracing messages
  * bpf: Make tracing capture len configurable

  [ Ian Vernon ]
  * pkg/bpfdebug: fix errant c/p in DumpInfo func desc

  [ Amre Shakimov ]
  * cilium-cni: move labels to ep
  * cni-plugin: pass through labels during ep creation

  [ Alexander Alemayhu ]
  * git: ignore more files
  * cmd: replace some of the placeholders with short descriptions
  * daemon: remove dead monitor code
  * pkg/byteorder: use Swap{16,32} in NetworkToHost
  * tests: fix typo

  [ Thomas Graf ]
  * monitor: Fix message separator output
  * labels: Include source in sortedList()

  [ Daniel Dao ]
  * fix overflow of arguments array

  [ André Martins ]
  * daemon: cleaning up leftover veths

  [ Daniel Borkmann ]
  * cilium: remove stale id also from policy map
  * cilium: also remove cilium_calls maps

  [ André Martins ]
  * Updating list of AUTHORS

  [ Amre Shakimov ]
  * fix kvstore arg for docker-compose example

  [ André Martins ]
  * tests: add k8s multi node to stress the L7 proxy

  [ Ian Vernon ]
  * tests: only change permissions of created logs dir

  [ Amre Shakimov ]
  * remove tmp debug file

  [ Eloy Coto ]
  * [Packaging] Fix deb packages build

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ André Martins ]
  * cni: ommiting errors for endpoint not found on DEL
  * cni: setting net.ipv6.conf.all.disable_ipv6=0

  [ Thomas Graf ]
  * k8s: Use existing `10-cilium.conf` CNI configuration if it exists
  * proxy: Create access log file if it does not exist

  [ John Fastabend ]
  * cilium: include versions in clang/kernel version error report

  [ peiqi ]
  * renew cilium policy setup script in examples

  [ John Fastabend ]
  * cilium: push version info into debug logs of epbf join

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ André Martins ]
  * cni: ommiting errors for endpoint not found on DEL
  * cni: setting net.ipv6.conf.all.disable_ipv6=0

  [ Thomas Graf ]
  * k8s: Use existing `10-cilium.conf` CNI configuration if it exists
  * proxy: Create access log file if it does not exist

  [ John Fastabend ]
  * cilium: include versions in clang/kernel version error report

  [ peiqi ]
  * renew cilium policy setup script in examples

  [ John Fastabend ]
  * cilium: push version info into debug logs of epbf join
  * cilium: data

  [ Eloy Coto ]
  * Monitor: omiting panic when no events Fix #757

  [ Jarno Rajahalme ]
  * tests: Show which test is running.
  * bpf: Add probe for LPM trie map type.
  * bpf: More rigorous bpf map probes.
  * policy: Add L3 rule validation and unit tests.
  * bpf: Support L3/IP access policy.
  * pkg/maps: Add support for flags in map creation.
  * pkg/maps: Add new cidrmap.
  * pkg/policy: Add L3 support.

  [ André Martins ]
  * policy: fix index out of range for MatchExpressions

  [ Alexander Alemayhu ]
  * tests: make link check ready for vagrant
  * Vagrantfile: add dependencies for building the docs
  * Documentation: add bpf updates 05

  [ John Fastabend ]
  * bpf: multiple ports in the policy causes compilation failure
  * cilium: merge ingress and egress proxy tests
  * cilium: add tests for with multiple l4 port maps
  * cilium: bindata

  [ Eloy Coto ]
  * BPF init: Check that cilium is present on the path fix #742 #743

  [ Steven Ceuppens ]
  * cilium: Move common/config.go to pkg/option/config.go - fix #760

  [ Thomas Graf ]
  * Update AUTHORS
  * Documentation: Provide installation instructions for key-value store
  * labels: Support regular expressions in label filters

  [ Sergey Generalov ]
  * New Cilium diagrams for the main README

  [ Steven Ceuppens ]
  * cilium: Change Consumable LabelList to LabelArray - fix #838

  [ Thomas Graf ]
  * k8s: Move pkg/k8s/types into pkg/k8s
  * labels: Move label source definition into labels package
  * labels: Rework of label source
  * Documentation: Provide documentation on labels

  [ Alexander Alemayhu ]
  * Makefile: add test target for applying changes
  * Vagrantfile: install go-bindata (Closes: #859)
  * cilium: fix outdated bindata

  [ Thomas Graf ]
  * Documentation: Add option to edit /etc/fstab

  [ Ian Vernon ]
  * tests: add K8s getting-started-guide test

  [ Michi Mutsuzaki ]
  * Fix L7 policy in the getting started guide

  [ Thomas Graf ]
  * Documentation: Document behaviour for existing pods
  * Documentation: Document minimal version of kubectl for ThirdPartyResources

  [ Jon Davies ]
  * cilium-ds.yaml: Added health checks to consul pods.

  [ André Martins ]
  * tests: add no-pager flag to journalctl

  [ Thomas Graf ]
  * k8s: Ignore headless services

  [ André Martins ]
  * plugins: fix add endpoint panic for len IDs less 5

  [ Thomas Graf ]
  * README refresher

  [ Madhu Challa ]
  * bpf: Encapsulation support for standalone loadbalancer

  [ Thomas Graf ]
  * bpf: Disable rp_filter on cilium_host
  * bpf: Update bindata
  * Documentation: Improve CNI configuration section

  [ Ian Vernon ]
  * daemon: update policy enforcement w/ no endpoints

  [ Thomas Graf ]
  * Force doc rebuild
  * Revert "bpf: Update bindata"
  * Revert "bpf: Disable rp_filter on cilium_host"
  * Revert "bpf: Encapsulation support for standalone loadbalancer"

  [ Ian Vernon ]
  * daemon: fix log message for kvstore opt

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding etcd version packages
  * Revert "etcd: checking for etcd version"
  * Revert "Godeps: adding etcd version packages"

  [ Tobias Klauser ]
  * Documentation: update cilium version to 0.9.0 in admin guide

  [ Alexander Alemayhu ]
  * Vagrantfile: install pip by default
  * Revert "tests: make link check ready for vagrant"

  [ Thomas Graf ]
  * bpf: Update bindata.go with go-bindata + go-1.8.3
  * tests: Disable go 1.8.3 specific unit test for now

  [ shijunqian ]
  * Documentation: fix clerical error

  [ Eloy Coto ]
  * CMD - service list command refactoring Fix #744

  [ André Martins ]
  * forcely setting UTC for Time.Now()

  [ Alexander Alemayhu ]
  * bpf/Makefile: add target for analyzing files

  [ Ian Vernon ]
  * tests/k8s/Vagrantfile: reset kubeadm before init
  * Vagrantfile: use different intnet per build

  [ Alexander Alemayhu ]
  * tests: add separate script for pulling in docker images

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding go-version from hashicorp

  [ Thomas Graf ]
  * proxy: Extended logging of requests
  * k8s: Add unit test for default deny rule

  [ Alexander Alemayhu ]
  * daemon: use endpoint specific error in endpoint TriggerPolicyUpdates
    (Closes: #677)

  [ Thomas Graf ]
  * vendor: Update vendor/github.com/vishvananda/netlink/

  [ Ian Vernon ]
  * daemon: default to empty string for key-value store

  [ Daniel Borkmann ]
  * cilium: add CiliumIPv6 StringNoZeroComp handler

  [ Thomas Graf ]
  * proxy: Log record format improvements
  * Documentation: Remove demo_docs as they trigger doc build errors
  * Documentation: Fix broken reference
  * kvstore: Move all code to pkg/kvstore and add singleton client

  [ André Martins ]
  * examples: update kubernetes example

  [ Alexander Alemayhu ]
  * Documentation: add blank lines after listing item

  [ Thomas Graf ]
  * endpoint: Factor our endpoint management into pkg/endpoint
  * nodeaddress: Move node address code into own package
  * proxy: Provide source and destination endpoint information as available

  [ Alexander Alemayhu ]
  * tests: reduce sleep calls

  [ Jonathan Davies ]
  * cilium-ds.yaml: Added healthchecks for cilium pods.

  [ Thomas Graf ]
  * AUTHORS: Add recent new authors

  [ Alexander Alemayhu ]
  * Documentation: minor nitpicks and rewording
  * Documentation: use git to set version info

  [ André Martins ]
  * kvstore: fix race conditions in GetWatchers

  [ Thomas Graf ]
  * proxy: Provide correct source endpoint ID
  * daemon: When BPF generation fails, log each output line separate warning
  * logging: Disable formatter to have simple format in TTY
  * daemon: Turn unnecessary info messages into debugging messages

  [ Tobias Klauser ]
  * daemon: remove unnecessary cgo integration

  [ Thomas Graf ]
  * proxy: Fix race when looking up endpoint metadata

  [ John Fastabend ]
  * cilium: proxy uses incorrect daddr when services are used
  * cilium: bindata

  [ Tobias Klauser ]
  * pkg/bpf: remove unnecessary cgo integration
  * pkg/bpf: add wrapper constants for bpf map types and flags

  [ Alexander Alemayhu ]
  * daemon: remove bindata from git
  * contrib: add script for checking bindata
  * daemon: track go version used for bindata updates
  * daemon: only use tracked files in go-bindata
  * Dockerfile: make adjustments now that bindata.go is gone

  [ André Martins ]
  * kvstore: erroring out with the default kvstore IPs
  * retrieving K8s PodCIDR from k8s-apiserver

  [ Ian Vernon ]
  * tests: gather /var/run/cilium, /var/lib/cilium as part of Jenkins builds

  [ Ross Guarino ]
  * docs(architecture): use /64 cluster prefix

  [ Amre Shakimov ]
  * add support for CNI 0.2.x spec

  [ André Martins ]
  * packaging: add .git to the list of files to copy
  * packaging: add scripts directory to the list of files to copy
  * tests: changed docker build to make docker-image
  * Vagrantfiles: excluding packaging docker/stage for rsync
  * Moved make clean from docker/Makefile to Dockerfile

  [ John Fastabend ]
  * cilium: add test for lb + proxy

  [ Thomas Graf ]
  * bindata: Remove auto commit on apply
  * bindata: Store SHA and version in daemon/bpf.sha

  [ André Martins ]
  * add bindata.go to the list of ignore files
  * daemon: reoder flags and enhanced flags description

  [ craigbox ]
  * Update gettingstarted.rst for minikube 0.20

  [ Thomas Graf ]
  * bpf: Only create conntrack entries for TCP SYN packets

  [ André Martins ]
  * tests: add multinode k8s tests
  * Jenkins: enabled k8s multi-node tests on jenkins
  * tests: changing VMs conflicting IPs

  [ Tobias Klauser ]
  * Fix broken links to Kubernetes docs
  * pkg/bpf: get and store owner program type for maps

  [ Amre Shakimov ]
  * fix include in Makefile

  [ André Martins ]
  * daemon: returning clearer k8s error messages
  * Godeps: updating etcd client
  * etcd: make necessary changes for etcd 3.2.1

  [ Amre Shakimov ]
  * use \$(RM) instead of rm for cleaning

  [ Dan Wendlandt ]
  * Fix K8s typo in main README

  [ André Martins ]
  * k8s: removing verbose debug message

  [ Tobias Klauser ]
  * pkg/bpf: explicitly include stdint.h

  [ André Martins ]
  * tests: restoring bash trace in helpers

  [ Thomas Graf ]
  * tests: Wait for endpoints to become available before invoking netperf
  * tests: copy_files should never fail
  * k8s-tests: Use files of local git tree

  [ Alexander Alemayhu ]
  * cmd: add description for cilium command (Closes: #1049)

  [ Thomas Graf ]
  * tests/k8s: Make k8s-gsg-test.sh error out on failure
  * tests/k8s: Fix path to Kubernetes example files
  * tests/k8s: Increase memory to 2GB
  * tests/k8s: Use existing helper scripts to wait for state changes
  * Vagrantfile: Do not inherit DNS server of host
  * kubernetes: Add scheduler tolerations to schedule Cilium on NotReady nodes

  [ Alexander Alemayhu ]
  * cmd, model: add verbose field to IdentityContext (Closes: #693)

  [ Tobias Klauser ]
  * pkg/bpf: use ClockGettime from x/sys/unix instead of C wrapper

  [ André Martins ]
  * Vagrantfile: use build IDs as part of VMs names

  [ Thomas Graf ]
  * nodeaddress: Retrieve hostname if available
  * tests: Move documentation checks to the end
  * Dockerfile: Include bpf-map tool in container image
  * Vagrantfile: Include bpf-map tool in developer vagrant box

  [ André Martins ]
  * ct: add aliend_id in ct maps
  * CT: cleaning entries when new policy denies them

  [ Amre Shakimov ]
  * tools: fix Makefile for docker

  [ Thomas Graf ]
  * node: Split EndpointsCIDR into IPv4 and IPv6 field
  * bpf: Add NonPersistent flag to force removal of map on startup
  * bpf: Simplified encapsulation mode
  * cli: Command to print contents of local tunnel endpoint map
  * daemon: Use real node address of host
  * node: Populate local node in tunnel endpoint map
  * lxcmap: Support migration of map format
  * agent: Add --ipv6-range to set per node IPv6 allocation prefix
  * vagrant: Use --ipv6-range to set per node prefix
  * agent: Print IPv4/IPv6 node IPs when starting up
  * agent: Provide options to overwrite IPv4/IPv6 node address
  * agent: Derive real IPv6 node adress and split router address
  * tests/lb: Do not rely on cilium_host having a global scope addres
  * tests/proxy: Show return http codes
  * agent: Masquerade IPv4 packets which leave the host
  * bpf: Delay opening of maps to after mount
  * tests/k8s: Adjust k8s multi node to new multi node mode
  * agent: Add --ipv4-cluster-cidr-mask-size option
  * nodeaddress: Validate that cluster prefix is great then node alloc prefix
  * cni: Install cni loopback driver if not installed already

  [ Ian Vernon ]
  * tests: pick up k8s test logs, other enhancements

  [ Michi Mutsuzaki ]
  * Refactor k8s.Client

  [ André Martins ]
  * k8s: change daemon's k8s clientSet to Interface
  * k8s: add node IP CIDR in the annotations node

  [ Daniel Dao ]
  * move from godeps to https://github.com/LK4D4/vndr

  [ Thomas Graf ]
  * k8s: Correctly implement ClusterIP for type NodePort and LoadBalancer

  [ Daniel Dao ]
  * vendor.conf: update etcd to 3.2.1
  * remove unused dependencies detected by vndr
  * remove Godeps manifest since we moved to vndr

  [ Thomas Graf ]
  * examples/demo: Clean up star wars demo

  [ Alexander Alemayhu ]
  * tests: check printed container ids
  * pkg: introduce new package for handling byteorder
  * bpf: don't call bpf_htonl for LXC_IPV4
  * byteorder: add documentation file for byteorder package (Closes: #285, 16)

  [ Tobias Klauser ]
  * pkg/bpf: add new sock ops prog type

  [ André Martins ]
  * K8s: keeping namespace label set in cilium NP
  * tests: adding l7 stresstest on different namespaces
  * tests: ignoring .json files in 00-no-sleep.sh
  * Jenkinsfile: add timestamps to builds
  * endpointmanager: fixed remove locked for DockerEndpointID

  [ Eloy Coto ]
  * [BPF/probes]: Added kernel checks Fixes: #891

  [ André Martins ]
  * k8s: implemented CiliumNetworkPolicy multi rule
  * tests: add one more example to tests multi CNP enforcement
  * tests: adding nodeselector for bookinfo spec

  [ Ian Vernon ]
  * Documentation: add CoreOS getting-started-guide

  [ Thomas Graf ]
  * tests: Disable CT cleanup tests for now
  * policy: Introduce policy repository revision
  * api: Return policy revision via API
  * api: Regenerate API code
  * agent/client: Report policy revision via API and print in CLI
  * agent: Report repository revision as endpoint policy build
  * cilium: New 'cilium policy wait <revison>` command
  * tests: Add helpers to wait for all endpoints to run a policy revision
  * Makefile: run etcd 3.1.0 for unit tests
  * vagrant: Fail if cilium.service fails and print log

  [ Thomas Bachman ]
  * Fix typo in demo1.sh

  [ Thomas Graf ]
  * find-llvm.sh: Add fast track for common location
  * bpf: Support encapsulation for packets received from a netdev
  * daemon: Add options to specify service CIDR
  * daemon: Point cluster CIDR to cilium_host in encapsulation mode
  * daemon: Automatically reserve conflicting local IPv4 routes in alloc prefix
  * proxy: Provide original identity as SO_MARK
  * bpf: Add additional debugging for bpf_netdev
  * bpf: Compile bpf_host.o after ENCAP_IFINDEX has been derived
  * agent: Masquerade traffic from host into cilium cluster
  * bpf: Support reaching the local node from the overlay
  * bpf: Store source identity in proxy map
  * proxy: Inherit endpoint source identity from proxy map
  * bpf: Attach bpf_host.o at egress of cilium_host
  * tests: Fix 06-lb.sh
  * bpf: Redirect to HOST_IFINDEX when doing proxy redirect
  * tests: Remove multinode L7 tests workaround

  [ Ian Vernon ]
  * cmd: add output options for cilium monitor

  [ Thomas Graf ]
  * bpf: Provide identity in capture messages
  * bpfdebug: Improve DumpInfo() output
  * monitor: Decode layers unless disabled
  * monitor: Print separator between messages in non-info mode
  * bpf: Do not require DEBUG flag to emit basic tracing messages
  * bpf: Make tracing capture len configurable

  [ Ian Vernon ]
  * pkg/bpfdebug: fix errant c/p in DumpInfo func desc

  [ Amre Shakimov ]
  * cilium-cni: move labels to ep
  * cni-plugin: pass through labels during ep creation

  [ Alexander Alemayhu ]
  * git: ignore more files
  * cmd: replace some of the placeholders with short descriptions
  * daemon: remove dead monitor code
  * pkg/byteorder: use Swap{16,32} in NetworkToHost
  * tests: fix typo

  [ Thomas Graf ]
  * monitor: Fix message separator output
  * labels: Include source in sortedList()

  [ Daniel Dao ]
  * fix overflow of arguments array

  [ André Martins ]
  * daemon: cleaning up leftover veths

  [ Daniel Borkmann ]
  * cilium: remove stale id also from policy map
  * cilium: also remove cilium_calls maps

  [ André Martins ]
  * Updating list of AUTHORS

  [ Amre Shakimov ]
  * fix kvstore arg for docker-compose example

  [ André Martins ]
  * tests: add k8s multi node to stress the L7 proxy

  [ Ian Vernon ]
  * tests: only change permissions of created logs dir

  [ Amre Shakimov ]
  * remove tmp debug file

  [ Eloy Coto ]
  * [Packaging] Fix deb packages build

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ André Martins ]
  * cni: ommiting errors for endpoint not found on DEL
  * cni: setting net.ipv6.conf.all.disable_ipv6=0

  [ Thomas Graf ]
  * k8s: Use existing `10-cilium.conf` CNI configuration if it exists
  * proxy: Create access log file if it does not exist

  [ John Fastabend ]
  * cilium: include versions in clang/kernel version error report

  [ peiqi ]
  * renew cilium policy setup script in examples

  [ John Fastabend ]
  * cilium: push version info into debug logs of epbf join
  * cilium: data

  [ Eloy Coto ]
  * Monitor: omiting panic when no events Fix #757

  [ Jarno Rajahalme ]
  * tests: Show which test is running.
  * bpf: Add probe for LPM trie map type.
  * bpf: More rigorous bpf map probes.
  * policy: Add L3 rule validation and unit tests.
  * bpf: Support L3/IP access policy.
  * pkg/maps: Add support for flags in map creation.
  * pkg/maps: Add new cidrmap.
  * pkg/policy: Add L3 support.

  [ André Martins ]
  * policy: fix index out of range for MatchExpressions

  [ Alexander Alemayhu ]
  * tests: make link check ready for vagrant
  * Vagrantfile: add dependencies for building the docs
  * Documentation: add bpf updates 05

  [ John Fastabend ]
  * bpf: multiple ports in the policy causes compilation failure
  * cilium: merge ingress and egress proxy tests
  * cilium: add tests for with multiple l4 port maps
  * cilium: bindata

  [ Eloy Coto ]
  * BPF init: Check that cilium is present on the path fix #742 #743

  [ Steven Ceuppens ]
  * cilium: Move common/config.go to pkg/option/config.go - fix #760

  [ Thomas Graf ]
  * Update AUTHORS
  * Documentation: Provide installation instructions for key-value store
  * labels: Support regular expressions in label filters

  [ Sergey Generalov ]
  * New Cilium diagrams for the main README

  [ Steven Ceuppens ]
  * cilium: Change Consumable LabelList to LabelArray - fix #838

  [ Thomas Graf ]
  * k8s: Move pkg/k8s/types into pkg/k8s
  * labels: Move label source definition into labels package
  * labels: Rework of label source
  * Documentation: Provide documentation on labels

  [ Alexander Alemayhu ]
  * Makefile: add test target for applying changes
  * Vagrantfile: install go-bindata (Closes: #859)
  * cilium: fix outdated bindata

  [ Thomas Graf ]
  * Documentation: Add option to edit /etc/fstab

  [ Ian Vernon ]
  * tests: add K8s getting-started-guide test

  [ Michi Mutsuzaki ]
  * Fix L7 policy in the getting started guide

  [ Thomas Graf ]
  * Documentation: Document behaviour for existing pods
  * Documentation: Document minimal version of kubectl for ThirdPartyResources

  [ Jon Davies ]
  * cilium-ds.yaml: Added health checks to consul pods.

  [ André Martins ]
  * tests: add no-pager flag to journalctl

  [ Thomas Graf ]
  * k8s: Ignore headless services

  [ André Martins ]
  * plugins: fix add endpoint panic for len IDs less 5

  [ Thomas Graf ]
  * README refresher

  [ Madhu Challa ]
  * bpf: Encapsulation support for standalone loadbalancer

  [ Thomas Graf ]
  * bpf: Disable rp_filter on cilium_host
  * bpf: Update bindata
  * Documentation: Improve CNI configuration section

  [ Ian Vernon ]
  * daemon: update policy enforcement w/ no endpoints

  [ Thomas Graf ]
  * Force doc rebuild
  * Revert "bpf: Update bindata"
  * Revert "bpf: Disable rp_filter on cilium_host"
  * Revert "bpf: Encapsulation support for standalone loadbalancer"

  [ Ian Vernon ]
  * daemon: fix log message for kvstore opt

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding etcd version packages
  * Revert "etcd: checking for etcd version"
  * Revert "Godeps: adding etcd version packages"

  [ Tobias Klauser ]
  * Documentation: update cilium version to 0.9.0 in admin guide

  [ Alexander Alemayhu ]
  * Vagrantfile: install pip by default
  * Revert "tests: make link check ready for vagrant"

  [ Thomas Graf ]
  * bpf: Update bindata.go with go-bindata + go-1.8.3
  * tests: Disable go 1.8.3 specific unit test for now

  [ shijunqian ]
  * Documentation: fix clerical error

  [ Eloy Coto ]
  * CMD - service list command refactoring Fix #744

  [ André Martins ]
  * forcely setting UTC for Time.Now()

  [ Alexander Alemayhu ]
  * bpf/Makefile: add target for analyzing files

  [ Ian Vernon ]
  * tests/k8s/Vagrantfile: reset kubeadm before init
  * Vagrantfile: use different intnet per build

  [ Alexander Alemayhu ]
  * tests: add separate script for pulling in docker images

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding go-version from hashicorp

  [ Thomas Graf ]
  * proxy: Extended logging of requests
  * k8s: Add unit test for default deny rule

  [ Alexander Alemayhu ]
  * daemon: use endpoint specific error in endpoint TriggerPolicyUpdates
    (Closes: #677)

  [ Thomas Graf ]
  * vendor: Update vendor/github.com/vishvananda/netlink/

  [ Ian Vernon ]
  * daemon: default to empty string for key-value store

  [ Daniel Borkmann ]
  * cilium: add CiliumIPv6 StringNoZeroComp handler

  [ Thomas Graf ]
  * proxy: Log record format improvements
  * Documentation: Remove demo_docs as they trigger doc build errors
  * Documentation: Fix broken reference
  * kvstore: Move all code to pkg/kvstore and add singleton client

  [ André Martins ]
  * examples: update kubernetes example

  [ Alexander Alemayhu ]
  * Documentation: add blank lines after listing item

  [ Thomas Graf ]
  * endpoint: Factor our endpoint management into pkg/endpoint
  * nodeaddress: Move node address code into own package
  * proxy: Provide source and destination endpoint information as available

  [ Alexander Alemayhu ]
  * tests: reduce sleep calls

  [ Jonathan Davies ]
  * cilium-ds.yaml: Added healthchecks for cilium pods.

  [ Thomas Graf ]
  * AUTHORS: Add recent new authors

  [ Alexander Alemayhu ]
  * Documentation: minor nitpicks and rewording
  * Documentation: use git to set version info

  [ André Martins ]
  * kvstore: fix race conditions in GetWatchers

  [ Thomas Graf ]
  * proxy: Provide correct source endpoint ID
  * daemon: When BPF generation fails, log each output line separate warning
  * logging: Disable formatter to have simple format in TTY
  * daemon: Turn unnecessary info messages into debugging messages

  [ Tobias Klauser ]
  * daemon: remove unnecessary cgo integration

  [ Thomas Graf ]
  * proxy: Fix race when looking up endpoint metadata

  [ John Fastabend ]
  * cilium: proxy uses incorrect daddr when services are used
  * cilium: bindata

  [ Tobias Klauser ]
  * pkg/bpf: remove unnecessary cgo integration
  * pkg/bpf: add wrapper constants for bpf map types and flags

  [ Alexander Alemayhu ]
  * daemon: remove bindata from git
  * contrib: add script for checking bindata
  * daemon: track go version used for bindata updates
  * daemon: only use tracked files in go-bindata
  * Dockerfile: make adjustments now that bindata.go is gone

  [ André Martins ]
  * kvstore: erroring out with the default kvstore IPs
  * retrieving K8s PodCIDR from k8s-apiserver

  [ Ian Vernon ]
  * tests: gather /var/run/cilium, /var/lib/cilium as part of Jenkins builds

  [ Ross Guarino ]
  * docs(architecture): use /64 cluster prefix

  [ Amre Shakimov ]
  * add support for CNI 0.2.x spec

  [ André Martins ]
  * packaging: add .git to the list of files to copy
  * packaging: add scripts directory to the list of files to copy
  * tests: changed docker build to make docker-image
  * Vagrantfiles: excluding packaging docker/stage for rsync
  * Moved make clean from docker/Makefile to Dockerfile

  [ John Fastabend ]
  * cilium: add test for lb + proxy

  [ Thomas Graf ]
  * bindata: Remove auto commit on apply
  * bindata: Store SHA and version in daemon/bpf.sha

  [ André Martins ]
  * add bindata.go to the list of ignore files
  * daemon: reoder flags and enhanced flags description

  [ craigbox ]
  * Update gettingstarted.rst for minikube 0.20

  [ Thomas Graf ]
  * bpf: Only create conntrack entries for TCP SYN packets

  [ André Martins ]
  * tests: add multinode k8s tests
  * Jenkins: enabled k8s multi-node tests on jenkins
  * tests: changing VMs conflicting IPs

  [ Tobias Klauser ]
  * Fix broken links to Kubernetes docs
  * pkg/bpf: get and store owner program type for maps

  [ Amre Shakimov ]
  * fix include in Makefile

  [ André Martins ]
  * daemon: returning clearer k8s error messages
  * Godeps: updating etcd client
  * etcd: make necessary changes for etcd 3.2.1

  [ Amre Shakimov ]
  * use \$(RM) instead of rm for cleaning

  [ Dan Wendlandt ]
  * Fix K8s typo in main README

  [ André Martins ]
  * k8s: removing verbose debug message

  [ Tobias Klauser ]
  * pkg/bpf: explicitly include stdint.h

  [ André Martins ]
  * tests: restoring bash trace in helpers

  [ Thomas Graf ]
  * tests: Wait for endpoints to become available before invoking netperf
  * tests: copy_files should never fail
  * k8s-tests: Use files of local git tree

  [ Alexander Alemayhu ]
  * cmd: add description for cilium command (Closes: #1049)

  [ Thomas Graf ]
  * tests/k8s: Make k8s-gsg-test.sh error out on failure
  * tests/k8s: Fix path to Kubernetes example files
  * tests/k8s: Increase memory to 2GB
  * tests/k8s: Use existing helper scripts to wait for state changes
  * Vagrantfile: Do not inherit DNS server of host
  * kubernetes: Add scheduler tolerations to schedule Cilium on NotReady nodes

  [ Alexander Alemayhu ]
  * cmd, model: add verbose field to IdentityContext (Closes: #693)

  [ Tobias Klauser ]
  * pkg/bpf: use ClockGettime from x/sys/unix instead of C wrapper

  [ André Martins ]
  * Vagrantfile: use build IDs as part of VMs names

  [ Thomas Graf ]
  * nodeaddress: Retrieve hostname if available
  * tests: Move documentation checks to the end
  * Dockerfile: Include bpf-map tool in container image
  * Vagrantfile: Include bpf-map tool in developer vagrant box

  [ André Martins ]
  * ct: add aliend_id in ct maps
  * CT: cleaning entries when new policy denies them

  [ Amre Shakimov ]
  * tools: fix Makefile for docker

  [ Thomas Graf ]
  * node: Split EndpointsCIDR into IPv4 and IPv6 field
  * bpf: Add NonPersistent flag to force removal of map on startup
  * bpf: Simplified encapsulation mode
  * cli: Command to print contents of local tunnel endpoint map
  * daemon: Use real node address of host
  * node: Populate local node in tunnel endpoint map
  * lxcmap: Support migration of map format
  * agent: Add --ipv6-range to set per node IPv6 allocation prefix
  * vagrant: Use --ipv6-range to set per node prefix
  * agent: Print IPv4/IPv6 node IPs when starting up
  * agent: Provide options to overwrite IPv4/IPv6 node address
  * agent: Derive real IPv6 node adress and split router address
  * tests/lb: Do not rely on cilium_host having a global scope addres
  * tests/proxy: Show return http codes
  * agent: Masquerade IPv4 packets which leave the host
  * bpf: Delay opening of maps to after mount
  * tests/k8s: Adjust k8s multi node to new multi node mode
  * agent: Add --ipv4-cluster-cidr-mask-size option
  * nodeaddress: Validate that cluster prefix is great then node alloc prefix
  * cni: Install cni loopback driver if not installed already

  [ Ian Vernon ]
  * tests: pick up k8s test logs, other enhancements

  [ Michi Mutsuzaki ]
  * Refactor k8s.Client

  [ André Martins ]
  * k8s: change daemon's k8s clientSet to Interface
  * k8s: add node IP CIDR in the annotations node

  [ Daniel Dao ]
  * move from godeps to https://github.com/LK4D4/vndr

  [ Thomas Graf ]
  * k8s: Correctly implement ClusterIP for type NodePort and LoadBalancer

  [ Daniel Dao ]
  * vendor.conf: update etcd to 3.2.1
  * remove unused dependencies detected by vndr
  * remove Godeps manifest since we moved to vndr

  [ Thomas Graf ]
  * examples/demo: Clean up star wars demo

  [ Alexander Alemayhu ]
  * tests: check printed container ids
  * pkg: introduce new package for handling byteorder
  * bpf: don't call bpf_htonl for LXC_IPV4
  * byteorder: add documentation file for byteorder package (Closes: #285, 16)

  [ Tobias Klauser ]
  * pkg/bpf: add new sock ops prog type

  [ André Martins ]
  * K8s: keeping namespace label set in cilium NP
  * tests: adding l7 stresstest on different namespaces
  * tests: ignoring .json files in 00-no-sleep.sh
  * Jenkinsfile: add timestamps to builds
  * endpointmanager: fixed remove locked for DockerEndpointID

  [ Eloy Coto ]
  * [BPF/probes]: Added kernel checks Fixes: #891

  [ André Martins ]
  * k8s: implemented CiliumNetworkPolicy multi rule
  * tests: add one more example to tests multi CNP enforcement
  * tests: adding nodeselector for bookinfo spec

  [ Ian Vernon ]
  * Documentation: add CoreOS getting-started-guide

  [ Thomas Graf ]
  * tests: Disable CT cleanup tests for now
  * policy: Introduce policy repository revision
  * api: Return policy revision via API
  * api: Regenerate API code
  * agent/client: Report policy revision via API and print in CLI
  * agent: Report repository revision as endpoint policy build
  * cilium: New 'cilium policy wait <revison>` command
  * tests: Add helpers to wait for all endpoints to run a policy revision
  * Makefile: run etcd 3.1.0 for unit tests
  * vagrant: Fail if cilium.service fails and print log

  [ Thomas Bachman ]
  * Fix typo in demo1.sh

  [ Thomas Graf ]
  * find-llvm.sh: Add fast track for common location
  * bpf: Support encapsulation for packets received from a netdev
  * daemon: Add options to specify service CIDR
  * daemon: Point cluster CIDR to cilium_host in encapsulation mode
  * daemon: Automatically reserve conflicting local IPv4 routes in alloc prefix
  * proxy: Provide original identity as SO_MARK
  * bpf: Add additional debugging for bpf_netdev
  * bpf: Compile bpf_host.o after ENCAP_IFINDEX has been derived
  * agent: Masquerade traffic from host into cilium cluster
  * bpf: Support reaching the local node from the overlay
  * bpf: Store source identity in proxy map
  * proxy: Inherit endpoint source identity from proxy map
  * bpf: Attach bpf_host.o at egress of cilium_host
  * tests: Fix 06-lb.sh
  * bpf: Redirect to HOST_IFINDEX when doing proxy redirect
  * tests: Remove multinode L7 tests workaround

  [ Ian Vernon ]
  * cmd: add output options for cilium monitor

  [ Thomas Graf ]
  * bpf: Provide identity in capture messages
  * bpfdebug: Improve DumpInfo() output
  * monitor: Decode layers unless disabled
  * monitor: Print separator between messages in non-info mode
  * bpf: Do not require DEBUG flag to emit basic tracing messages
  * bpf: Make tracing capture len configurable

  [ Ian Vernon ]
  * pkg/bpfdebug: fix errant c/p in DumpInfo func desc

  [ Amre Shakimov ]
  * cilium-cni: move labels to ep
  * cni-plugin: pass through labels during ep creation

  [ Alexander Alemayhu ]
  * git: ignore more files
  * cmd: replace some of the placeholders with short descriptions
  * daemon: remove dead monitor code
  * pkg/byteorder: use Swap{16,32} in NetworkToHost
  * tests: fix typo

  [ Thomas Graf ]
  * monitor: Fix message separator output
  * labels: Include source in sortedList()

  [ Daniel Dao ]
  * fix overflow of arguments array

  [ André Martins ]
  * daemon: cleaning up leftover veths

  [ Daniel Borkmann ]
  * cilium: remove stale id also from policy map
  * cilium: also remove cilium_calls maps

  [ André Martins ]
  * Updating list of AUTHORS

  [ Amre Shakimov ]
  * fix kvstore arg for docker-compose example

  [ André Martins ]
  * tests: add k8s multi node to stress the L7 proxy

  [ Ian Vernon ]
  * tests: only change permissions of created logs dir

  [ Amre Shakimov ]
  * remove tmp debug file

  [ Eloy Coto ]
  * [Packaging] Fix deb packages build

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ André Martins ]
  * cni: ommiting errors for endpoint not found on DEL
  * cni: setting net.ipv6.conf.all.disable_ipv6=0

  [ Thomas Graf ]
  * k8s: Use existing `10-cilium.conf` CNI configuration if it exists
  * proxy: Create access log file if it does not exist

  [ John Fastabend ]
  * cilium: include versions in clang/kernel version error report

  [ peiqi ]
  * renew cilium policy setup script in examples

  [ John Fastabend ]
  * cilium: push version info into debug logs of epbf join
  * cilium: data

  [ Eloy Coto ]
  * Monitor: omiting panic when no events Fix #757

  [ Jarno Rajahalme ]
  * tests: Show which test is running.
  * bpf: Add probe for LPM trie map type.
  * bpf: More rigorous bpf map probes.
  * policy: Add L3 rule validation and unit tests.
  * bpf: Support L3/IP access policy.
  * pkg/maps: Add support for flags in map creation.
  * pkg/maps: Add new cidrmap.
  * pkg/policy: Add L3 support.

  [ André Martins ]
  * policy: fix index out of range for MatchExpressions

  [ Alexander Alemayhu ]
  * tests: make link check ready for vagrant
  * Vagrantfile: add dependencies for building the docs
  * Documentation: add bpf updates 05

  [ John Fastabend ]
  * bpf: multiple ports in the policy causes compilation failure
  * cilium: merge ingress and egress proxy tests
  * cilium: add tests for with multiple l4 port maps
  * cilium: bindata

  [ Eloy Coto ]
  * BPF init: Check that cilium is present on the path fix #742 #743

  [ Steven Ceuppens ]
  * cilium: Move common/config.go to pkg/option/config.go - fix #760

  [ Thomas Graf ]
  * Update AUTHORS
  * Documentation: Provide installation instructions for key-value store
  * labels: Support regular expressions in label filters

  [ Sergey Generalov ]
  * New Cilium diagrams for the main README

  [ Steven Ceuppens ]
  * cilium: Change Consumable LabelList to LabelArray - fix #838

  [ Thomas Graf ]
  * k8s: Move pkg/k8s/types into pkg/k8s
  * labels: Move label source definition into labels package
  * labels: Rework of label source
  * Documentation: Provide documentation on labels

  [ Alexander Alemayhu ]
  * Makefile: add test target for applying changes
  * Vagrantfile: install go-bindata (Closes: #859)
  * cilium: fix outdated bindata

  [ Thomas Graf ]
  * Documentation: Add option to edit /etc/fstab

  [ Ian Vernon ]
  * tests: add K8s getting-started-guide test

  [ Michi Mutsuzaki ]
  * Fix L7 policy in the getting started guide

  [ Thomas Graf ]
  * Documentation: Document behaviour for existing pods
  * Documentation: Document minimal version of kubectl for ThirdPartyResources

  [ Jon Davies ]
  * cilium-ds.yaml: Added health checks to consul pods.

  [ André Martins ]
  * tests: add no-pager flag to journalctl

  [ Thomas Graf ]
  * k8s: Ignore headless services

  [ André Martins ]
  * plugins: fix add endpoint panic for len IDs less 5

  [ Thomas Graf ]
  * README refresher

  [ Madhu Challa ]
  * bpf: Encapsulation support for standalone loadbalancer

  [ Thomas Graf ]
  * bpf: Disable rp_filter on cilium_host
  * bpf: Update bindata
  * Documentation: Improve CNI configuration section

  [ Ian Vernon ]
  * daemon: update policy enforcement w/ no endpoints

  [ Thomas Graf ]
  * Force doc rebuild
  * Revert "bpf: Update bindata"
  * Revert "bpf: Disable rp_filter on cilium_host"
  * Revert "bpf: Encapsulation support for standalone loadbalancer"

  [ Ian Vernon ]
  * daemon: fix log message for kvstore opt

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding etcd version packages
  * Revert "etcd: checking for etcd version"
  * Revert "Godeps: adding etcd version packages"

  [ Tobias Klauser ]
  * Documentation: update cilium version to 0.9.0 in admin guide

  [ Alexander Alemayhu ]
  * Vagrantfile: install pip by default
  * Revert "tests: make link check ready for vagrant"

  [ Thomas Graf ]
  * bpf: Update bindata.go with go-bindata + go-1.8.3
  * tests: Disable go 1.8.3 specific unit test for now

  [ shijunqian ]
  * Documentation: fix clerical error

  [ Eloy Coto ]
  * CMD - service list command refactoring Fix #744

  [ André Martins ]
  * forcely setting UTC for Time.Now()

  [ Alexander Alemayhu ]
  * bpf/Makefile: add target for analyzing files

  [ Ian Vernon ]
  * tests/k8s/Vagrantfile: reset kubeadm before init
  * Vagrantfile: use different intnet per build

  [ Alexander Alemayhu ]
  * tests: add separate script for pulling in docker images

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding go-version from hashicorp

  [ Thomas Graf ]
  * proxy: Extended logging of requests
  * k8s: Add unit test for default deny rule

  [ Alexander Alemayhu ]
  * daemon: use endpoint specific error in endpoint TriggerPolicyUpdates
    (Closes: #677)

  [ Thomas Graf ]
  * vendor: Update vendor/github.com/vishvananda/netlink/

  [ Ian Vernon ]
  * daemon: default to empty string for key-value store

  [ Daniel Borkmann ]
  * cilium: add CiliumIPv6 StringNoZeroComp handler

  [ Thomas Graf ]
  * proxy: Log record format improvements
  * Documentation: Remove demo_docs as they trigger doc build errors
  * Documentation: Fix broken reference
  * kvstore: Move all code to pkg/kvstore and add singleton client

  [ André Martins ]
  * examples: update kubernetes example

  [ Alexander Alemayhu ]
  * Documentation: add blank lines after listing item

  [ Thomas Graf ]
  * endpoint: Factor our endpoint management into pkg/endpoint
  * nodeaddress: Move node address code into own package
  * proxy: Provide source and destination endpoint information as available

  [ Alexander Alemayhu ]
  * tests: reduce sleep calls

  [ Jonathan Davies ]
  * cilium-ds.yaml: Added healthchecks for cilium pods.

  [ Thomas Graf ]
  * AUTHORS: Add recent new authors

  [ Alexander Alemayhu ]
  * Documentation: minor nitpicks and rewording
  * Documentation: use git to set version info

  [ André Martins ]
  * kvstore: fix race conditions in GetWatchers

  [ Thomas Graf ]
  * proxy: Provide correct source endpoint ID
  * daemon: When BPF generation fails, log each output line separate warning
  * logging: Disable formatter to have simple format in TTY
  * daemon: Turn unnecessary info messages into debugging messages

  [ Tobias Klauser ]
  * daemon: remove unnecessary cgo integration

  [ Thomas Graf ]
  * proxy: Fix race when looking up endpoint metadata

  [ John Fastabend ]
  * cilium: proxy uses incorrect daddr when services are used

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message
  * k8s/rbac: Grant access to componentstatues

  [ Dan Wendlandt ]
  * Minor formatting and graphic fixes for k8s getting started guide

  [ André Martins ]
  * cni: ommiting errors for endpoint not found on DEL
  * cni: setting net.ipv6.conf.all.disable_ipv6=0

  [ Thomas Graf ]
  * k8s: Use existing `10-cilium.conf` CNI configuration if it exists
  * proxy: Create access log file if it does not exist

  [ John Fastabend ]
  * cilium: include versions in clang/kernel version error report

  [ peiqi ]
  * renew cilium policy setup script in examples

  [ John Fastabend ]
  * cilium: push version info into debug logs of epbf join
  * cilium: data

  [ Eloy Coto ]
  * Monitor: omiting panic when no events Fix #757

  [ Jarno Rajahalme ]
  * tests: Show which test is running.
  * bpf: Add probe for LPM trie map type.
  * bpf: More rigorous bpf map probes.
  * policy: Add L3 rule validation and unit tests.
  * bpf: Support L3/IP access policy.
  * pkg/maps: Add support for flags in map creation.
  * pkg/maps: Add new cidrmap.
  * pkg/policy: Add L3 support.

  [ André Martins ]
  * policy: fix index out of range for MatchExpressions

  [ Alexander Alemayhu ]
  * tests: make link check ready for vagrant
  * Vagrantfile: add dependencies for building the docs
  * Documentation: add bpf updates 05

  [ John Fastabend ]
  * bpf: multiple ports in the policy causes compilation failure
  * cilium: merge ingress and egress proxy tests
  * cilium: add tests for with multiple l4 port maps
  * cilium: bindata

  [ Eloy Coto ]
  * BPF init: Check that cilium is present on the path fix #742 #743

  [ Steven Ceuppens ]
  * cilium: Move common/config.go to pkg/option/config.go - fix #760

  [ Thomas Graf ]
  * Update AUTHORS
  * Documentation: Provide installation instructions for key-value store
  * labels: Support regular expressions in label filters

  [ Sergey Generalov ]
  * New Cilium diagrams for the main README

  [ Steven Ceuppens ]
  * cilium: Change Consumable LabelList to LabelArray - fix #838

  [ Thomas Graf ]
  * k8s: Move pkg/k8s/types into pkg/k8s
  * labels: Move label source definition into labels package
  * labels: Rework of label source
  * Documentation: Provide documentation on labels

  [ Alexander Alemayhu ]
  * Makefile: add test target for applying changes
  * Vagrantfile: install go-bindata (Closes: #859)
  * cilium: fix outdated bindata

  [ Thomas Graf ]
  * Documentation: Add option to edit /etc/fstab

  [ Ian Vernon ]
  * tests: add K8s getting-started-guide test

  [ Michi Mutsuzaki ]
  * Fix L7 policy in the getting started guide

  [ Thomas Graf ]
  * Documentation: Document behaviour for existing pods
  * Documentation: Document minimal version of kubectl for ThirdPartyResources

  [ Jon Davies ]
  * cilium-ds.yaml: Added health checks to consul pods.

  [ André Martins ]
  * tests: add no-pager flag to journalctl

  [ Thomas Graf ]
  * k8s: Ignore headless services

  [ André Martins ]
  * plugins: fix add endpoint panic for len IDs less 5

  [ Thomas Graf ]
  * README refresher

  [ Madhu Challa ]
  * bpf: Encapsulation support for standalone loadbalancer

  [ Thomas Graf ]
  * bpf: Disable rp_filter on cilium_host
  * bpf: Update bindata
  * Documentation: Improve CNI configuration section

  [ Ian Vernon ]
  * daemon: update policy enforcement w/ no endpoints

  [ Thomas Graf ]
  * Force doc rebuild
  * Revert "bpf: Update bindata"
  * Revert "bpf: Disable rp_filter on cilium_host"
  * Revert "bpf: Encapsulation support for standalone loadbalancer"

  [ Ian Vernon ]
  * daemon: fix log message for kvstore opt

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding etcd version packages
  * Revert "etcd: checking for etcd version"
  * Revert "Godeps: adding etcd version packages"

  [ Tobias Klauser ]
  * Documentation: update cilium version to 0.9.0 in admin guide

  [ Alexander Alemayhu ]
  * Vagrantfile: install pip by default
  * Revert "tests: make link check ready for vagrant"

  [ Thomas Graf ]
  * bpf: Update bindata.go with go-bindata + go-1.8.3
  * tests: Disable go 1.8.3 specific unit test for now

  [ shijunqian ]
  * Documentation: fix clerical error

  [ Eloy Coto ]
  * CMD - service list command refactoring Fix #744

  [ André Martins ]
  * forcely setting UTC for Time.Now()

  [ Alexander Alemayhu ]
  * bpf/Makefile: add target for analyzing files

  [ Ian Vernon ]
  * tests/k8s/Vagrantfile: reset kubeadm before init
  * Vagrantfile: use different intnet per build

  [ Alexander Alemayhu ]
  * tests: add separate script for pulling in docker images

  [ André Martins ]
  * etcd: checking for etcd version
  * Godeps: adding go-version from hashicorp

  [ Thomas Graf ]
  * proxy: Extended logging of requests
  * k8s: Add unit test for default deny rule

  [ Alexander Alemayhu ]
  * daemon: use endpoint specific error in endpoint TriggerPolicyUpdates
    (Closes: #677)

  [ Thomas Graf ]
  * vendor: Update vendor/github.com/vishvananda/netlink/

  [ Ian Vernon ]
  * daemon: default to empty string for key-value store

  [ Daniel Borkmann ]
  * cilium: add CiliumIPv6 StringNoZeroComp handler

  [ Thomas Graf ]
  * proxy: Log record format improvements
  * Documentation: Remove demo_docs as they trigger doc build errors
  * Documentation: Fix broken reference
  * kvstore: Move all code to pkg/kvstore and add singleton client

  [ André Martins ]
  * examples: update kubernetes example

  [ Alexander Alemayhu ]
  * Documentation: add blank lines after listing item

  [ Thomas Graf ]
  * endpoint: Factor our endpoint management into pkg/endpoint
  * nodeaddress: Move node address code into own package
  * proxy: Provide source and destination endpoint information as available

  [ Alexander Alemayhu ]
  * tests: reduce sleep calls

  [ Jonathan Davies ]
  * cilium-ds.yaml: Added healthchecks for cilium pods.

  [ Thomas Graf ]
  * AUTHORS: Add recent new authors

  [ Alexander Alemayhu ]
  * Documentation: minor nitpicks and rewording
  * Documentation: use git to set version info

  [ André Martins ]
  * kvstore: fix race conditions in GetWatchers

  [ Thomas Graf ]
  * proxy: Provide correct source endpoint ID
  * daemon: When BPF generation fails, log each output line separate warning
  * logging: Disable formatter to have simple format in TTY
  * daemon: Turn unnecessary info messages into debugging messages

  [ Tobias Klauser ]
  * daemon: remove unnecessary cgo integration

  [ Thomas Graf ]
  * proxy: Fix race when looking up endpoint metadata

  [ John Fastabend ]
  * cilium: proxy uses incorrect daddr when services are used
  * cilium: bindata

  [ Tobias Klauser ]
  * pkg/bpf: remove unnecessary cgo integration
  * pkg/bpf: add wrapper constants for bpf map types and flags

  [ Alexander Alemayhu ]
  * daemon: remove bindata from git
  * contrib: add script for checking bindata
  * daemon: track go version used for bindata updates
  * daemon: only use tracked files in go-bindata
  * Dockerfile: make adjustments now that bindata.go is gone

  [ André Martins ]
  * kvstore: erroring out with the default kvstore IPs
  * retrieving K8s PodCIDR from k8s-apiserver

  [ Ian Vernon ]
  * tests: gather /var/run/cilium, /var/lib/cilium as part of Jenkins builds

  [ Ross Guarino ]
  * docs(architecture): use /64 cluster prefix

  [ Amre Shakimov ]
  * add support for CNI 0.2.x spec

  [ André Martins ]
  * packaging: add .git to the list of files to copy
  * packaging: add scripts directory to the list of files to copy
  * tests: changed docker build to make docker-image
  * Vagrantfiles: excluding packaging docker/stage for rsync
  * Moved make clean from docker/Makefile to Dockerfile

  [ John Fastabend ]
  * cilium: add test for lb + proxy

  [ Thomas Graf ]
  * bindata: Remove auto commit on apply
  * bindata: Store SHA and version in daemon/bpf.sha

  [ André Martins ]
  * add bindata.go to the list of ignore files
  * daemon: reoder flags and enhanced flags description

  [ craigbox ]
  * Update gettingstarted.rst for minikube 0.20

  [ Thomas Graf ]
  * bpf: Only create conntrack entries for TCP SYN packets

  [ André Martins ]
  * tests: add multinode k8s tests
  * Jenkins: enabled k8s multi-node tests on jenkins
  * tests: changing VMs conflicting IPs

  [ Tobias Klauser ]
  * Fix broken links to Kubernetes docs
  * pkg/bpf: get and store owner program type for maps

  [ Amre Shakimov ]
  * fix include in Makefile

  [ André Martins ]
  * daemon: returning clearer k8s error messages
  * Godeps: updating etcd client
  * etcd: make necessary changes for etcd 3.2.1

  [ Amre Shakimov ]
  * use \$(RM) instead of rm for cleaning

  [ Dan Wendlandt ]
  * Fix K8s typo in main README

  [ André Martins ]
  * k8s: removing verbose debug message

  [ Tobias Klauser ]
  * pkg/bpf: explicitly include stdint.h

  [ André Martins ]
  * tests: restoring bash trace in helpers

  [ Thomas Graf ]
  * tests: Wait for endpoints to become available before invoking netperf
  * tests: copy_files should never fail
  * k8s-tests: Use files of local git tree

  [ Alexander Alemayhu ]
  * cmd: add description for cilium command (Closes: #1049)

  [ Thomas Graf ]
  * tests/k8s: Make k8s-gsg-test.sh error out on failure
  * tests/k8s: Fix path to Kubernetes example files
  * tests/k8s: Increase memory to 2GB
  * tests/k8s: Use existing helper scripts to wait for state changes
  * Vagrantfile: Do not inherit DNS server of host
  * kubernetes: Add scheduler tolerations to schedule Cilium on NotReady nodes

  [ Alexander Alemayhu ]
  * cmd, model: add verbose field to IdentityContext (Closes: #693)

  [ Tobias Klauser ]
  * pkg/bpf: use ClockGettime from x/sys/unix instead of C wrapper

  [ André Martins ]
  * Vagrantfile: use build IDs as part of VMs names

  [ Thomas Graf ]
  * nodeaddress: Retrieve hostname if available
  * tests: Move documentation checks to the end
  * Dockerfile: Include bpf-map tool in container image
  * Vagrantfile: Include bpf-map tool in developer vagrant box

  [ André Martins ]
  * ct: add aliend_id in ct maps
  * CT: cleaning entries when new policy denies them

  [ Amre Shakimov ]
  * tools: fix Makefile for docker

  [ Thomas Graf ]
  * node: Split EndpointsCIDR into IPv4 and IPv6 field
  * bpf: Add NonPersistent flag to force removal of map on startup
  * bpf: Simplified encapsulation mode
  * cli: Command to print contents of local tunnel endpoint map
  * daemon: Use real node address of host
  * node: Populate local node in tunnel endpoint map
  * lxcmap: Support migration of map format
  * agent: Add --ipv6-range to set per node IPv6 allocation prefix
  * vagrant: Use --ipv6-range to set per node prefix
  * agent: Print IPv4/IPv6 node IPs when starting up
  * agent: Provide options to overwrite IPv4/IPv6 node address
  * agent: Derive real IPv6 node adress and split router address
  * tests/lb: Do not rely on cilium_host having a global scope addres
  * tests/proxy: Show return http codes
  * agent: Masquerade IPv4 packets which leave the host
  * bpf: Delay opening of maps to after mount
  * tests/k8s: Adjust k8s multi node to new multi node mode
  * agent: Add --ipv4-cluster-cidr-mask-size option
  * nodeaddress: Validate that cluster prefix is great then node alloc prefix
  * cni: Install cni loopback driver if not installed already

  [ Ian Vernon ]
  * tests: pick up k8s test logs, other enhancements

  [ Michi Mutsuzaki ]
  * Refactor k8s.Client

  [ André Martins ]
  * k8s: change daemon's k8s clientSet to Interface
  * k8s: add node IP CIDR in the annotations node

  [ Daniel Dao ]
  * move from godeps to https://github.com/LK4D4/vndr

  [ Thomas Graf ]
  * k8s: Correctly implement ClusterIP for type NodePort and LoadBalancer

  [ Daniel Dao ]
  * vendor.conf: update etcd to 3.2.1
  * remove unused dependencies detected by vndr
  * remove Godeps manifest since we moved to vndr

  [ Thomas Graf ]
  * examples/demo: Clean up star wars demo

  [ Alexander Alemayhu ]
  * tests: check printed container ids
  * pkg: introduce new package for handling byteorder
  * bpf: don't call bpf_htonl for LXC_IPV4
  * byteorder: add documentation file for byteorder package (Closes: #285, 16)

  [ Tobias Klauser ]
  * pkg/bpf: add new sock ops prog type

  [ André Martins ]
  * K8s: keeping namespace label set in cilium NP
  * tests: adding l7 stresstest on different namespaces
  * tests: ignoring .json files in 00-no-sleep.sh
  * Jenkinsfile: add timestamps to builds
  * endpointmanager: fixed remove locked for DockerEndpointID

  [ Eloy Coto ]
  * [BPF/probes]: Added kernel checks Fixes: #891

  [ André Martins ]
  * k8s: implemented CiliumNetworkPolicy multi rule
  * tests: add one more example to tests multi CNP enforcement
  * tests: adding nodeselector for bookinfo spec

  [ Ian Vernon ]
  * Documentation: add CoreOS getting-started-guide

  [ Thomas Graf ]
  * tests: Disable CT cleanup tests for now
  * policy: Introduce policy repository revision
  * api: Return policy revision via API
  * api: Regenerate API code
  * agent/client: Report policy revision via API and print in CLI
  * agent: Report repository revision as endpoint policy build
  * cilium: New 'cilium policy wait <revison>` command
  * tests: Add helpers to wait for all endpoints to run a policy revision
  * Makefile: run etcd 3.1.0 for unit tests
  * vagrant: Fail if cilium.service fails and print log

  [ Thomas Bachman ]
  * Fix typo in demo1.sh

  [ Thomas Graf ]
  * find-llvm.sh: Add fast track for common location
  * bpf: Support encapsulation for packets received from a netdev
  * daemon: Add options to specify service CIDR
  * daemon: Point cluster CIDR to cilium_host in encapsulation mode
  * daemon: Automatically reserve conflicting local IPv4 routes in alloc prefix
  * proxy: Provide original identity as SO_MARK
  * bpf: Add additional debugging for bpf_netdev
  * bpf: Compile bpf_host.o after ENCAP_IFINDEX has been derived
  * agent: Masquerade traffic from host into cilium cluster
  * bpf: Support reaching the local node from the overlay
  * bpf: Store source identity in proxy map
  * proxy: Inherit endpoint source identity from proxy map
  * bpf: Attach bpf_host.o at egress of cilium_host
  * tests: Fix 06-lb.sh
  * bpf: Redirect to HOST_IFINDEX when doing proxy redirect
  * tests: Remove multinode L7 tests workaround

  [ Ian Vernon ]
  * cmd: add output options for cilium monitor

  [ Thomas Graf ]
  * bpf: Provide identity in capture messages
  * bpfdebug: Improve DumpInfo() output
  * monitor: Decode layers unless disabled
  * monitor: Print separator between messages in non-info mode
  * bpf: Do not require DEBUG flag to emit basic tracing messages
  * bpf: Make tracing capture len configurable

  [ Ian Vernon ]
  * pkg/bpfdebug: fix errant c/p in DumpInfo func desc

  [ Amre Shakimov ]
  * cilium-cni: move labels to ep
  * cni-plugin: pass through labels during ep creation

  [ Alexander Alemayhu ]
  * git: ignore more files
  * cmd: replace some of the placeholders with short descriptions
  * daemon: remove dead monitor code
  * pkg/byteorder: use Swap{16,32} in NetworkToHost
  * tests: fix typo

  [ Thomas Graf ]
  * monitor: Fix message separator output
  * labels: Include source in sortedList()

  [ Daniel Dao ]
  * fix overflow of arguments array

  [ André Martins ]
  * daemon: cleaning up leftover veths

  [ Daniel Borkmann ]
  * cilium: remove stale id also from policy map
  * cilium: also remove cilium_calls maps

  [ André Martins ]
  * Updating list of AUTHORS

  [ Amre Shakimov ]
  * fix kvstore arg for docker-compose example

  [ André Martins ]
  * tests: add k8s multi node to stress the L7 proxy

  [ Ian Vernon ]
  * tests: only change permissions of created logs dir

  [ Amre Shakimov ]
  * remove tmp debug file

  [ Eloy Coto ]
  * [Packaging] Fix deb packages build
  * Version 0.9.90

  [ Thomas Graf ]
  * Prepare development branch after release
  * k8s: Specify ClusterRoleBinding group
  * cilium: Print component status message

 -- Thomas Graf <thomas@cilium.io>  Wed, 19 Jul 2017 20:27:06 +0000
