cluster_config:
  idle_timeout_seconds: 60
  use_app_protocol: true
host_network_config: {}
ip_config:
  ipv4_enabled: true
  ipv6_enabled: true
listener_config:
  stream_idle_timeout_seconds: 300
original_ip_detection_config: {}
route_config:
  host_name_suffix_match: true
secrets_namespace: cilium-secrets
