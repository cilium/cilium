apiVersion: "cilium.io/v2"
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: "host-cluster-egress-ccnp"
spec:
  endpointSelector:
    matchLabels: {}
  egress:
    - toEntities:
        - host
        - remote-node
