apiVersion: "cilium.io/v2"
kind: CiliumClusterwideNetworkPolicy
metadata:
  name: "entity-cluster-ccnp"
spec:
  endpointSelector:
    matchLabels:
      kind: client
  egress:
    - toEntities:
        - cluster
