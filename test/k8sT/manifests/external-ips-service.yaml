apiVersion: v1
kind: Service
metadata:
  name: external-ips-service
  labels:
    zgroup: testapp
    id: app1
spec:
  selector:
    zgroup: testapp
    id: app1
  externalIPs:
  - 192.0.2.233
  ports:
  - protocol: TCP
    port: 82
    targetPort: 80
