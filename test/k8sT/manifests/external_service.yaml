apiVersion: v1
kind: Service
metadata:
  name: external-service
  labels:
    zgroup: external
    test: external
  namespace: default
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 9376
