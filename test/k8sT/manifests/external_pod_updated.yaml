apiVersion: v1
kind: Pod
metadata:
  name: toservices-updated
  labels:
    zgroup: external
    test: toservices-updated
spec:
  containers:
  - name: app-reach-services
    image: docker.io/cilium/demo-client:latest
    imagePullPolicy: IfNotPresent
    command: [ "bash", "-c", "while true; do sleep 5; done" ]
