{{- if eq (include "ztunnelSpireInstallEnabled" .) "true" -}}
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ .Values.encryption.ztunnel.spire.install.server.serviceAccount.name }}
  namespace: {{ .Values.encryption.ztunnel.spire.install.namespace }}
  {{- with .Values.commonLabels }}
  labels:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.encryption.ztunnel.spire.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: spire-server
subjects:
- kind: ServiceAccount
  name: {{ .Values.encryption.ztunnel.spire.install.server.serviceAccount.name }}
  namespace: {{ .Values.encryption.ztunnel.spire.install.namespace }}
---
kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ .Values.encryption.ztunnel.spire.install.server.serviceAccount.name }}-pod
  namespace: {{ .Values.encryption.ztunnel.spire.install.namespace }}
  {{- with .Values.encryption.ztunnel.spire.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ .Values.encryption.ztunnel.spire.install.server.serviceAccount.name }}-pod
subjects:
- kind: ServiceAccount
  name: {{ .Values.encryption.ztunnel.spire.install.server.serviceAccount.name }}
  namespace: {{ .Values.encryption.ztunnel.spire.install.namespace }}
{{- end }}
