{{- if eq (include "ztunnelSpireInstallEnabled" .) "true" -}}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: spire-server
  namespace: {{ .Values.encryption.ztunnel.spire.install.namespace }}
  {{- if or .Values.encryption.ztunnel.spire.install.server.annotations .Values.encryption.ztunnel.spire.annotations }}
  annotations:
    {{- with .Values.encryption.ztunnel.spire.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.encryption.ztunnel.spire.install.server.annotations }}
      {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- end }}
  labels:
    app: spire-server
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    {{- with .Values.encryption.ztunnel.spire.install.server.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spire-server
  serviceName: spire-server
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: spire-server
      labels:
        app: spire-server
        {{- with .Values.commonLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- with .Values.encryption.ztunnel.spire.install.server.labels }}
          {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      priorityClassName: {{ include "cilium.priorityClass" (list $ .Values.encryption.ztunnel.spire.install.server.priorityClassName "system-node-critical") }}
      serviceAccountName: {{ .Values.encryption.ztunnel.spire.install.server.serviceAccount.name }}
      shareProcessNamespace: true
      {{- if (semverCompare ">= 1.33-0" .Capabilities.KubeVersion.Version) }}
      hostUsers: true
      {{- end }}
      {{- with .Values.encryption.ztunnel.spire.install.server.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- if gt (len .Values.encryption.ztunnel.spire.install.server.initContainers) 0 }}
      initContainers:
        {{- toYaml .Values.encryption.ztunnel.spire.install.server.initContainers | nindent 8 }}
      {{- end }}
      containers:
      - name: cilium-init
        image: {{ include "cilium.image" .Values.encryption.ztunnel.spire.install.initImage | quote }}
        imagePullPolicy: {{ .Values.encryption.ztunnel.spire.install.initImage.pullPolicy }}
        command:
          - /bin/sh
          - -c
          - |
            {{- tpl (.Files.Get "files/spire/init.bash") . | nindent 12 }}
      - name: spire-server
        {{- if eq (typeOf .Values.encryption.ztunnel.spire.install.server.image) "string" }}
        image: {{ .Values.encryption.ztunnel.spire.install.server.image }}
        {{- else }}
        image: {{ include "cilium.image" .Values.encryption.ztunnel.spire.install.server.image | quote }}
        imagePullPolicy: {{ .Values.encryption.ztunnel.spire.install.server.image.pullPolicy }}
        {{- end }}
        args:
        - -config
        - /run/spire/config/server.conf
        {{- with .Values.encryption.ztunnel.spire.install.server.resources }}
        resources:
          {{- toYaml . | trim | nindent 10 }}
        {{- end }}
        ports:
        - name: grpc
          containerPort: 8081
        - name: http
          containerPort: 8080
        volumeMounts:
        - name: spire-config
          mountPath: /run/spire/config
          readOnly: true
        {{- if .Values.encryption.ztunnel.spire.install.server.dataStorage.enabled }}
        - name: spire-data
          mountPath: /run/spire/data
          readOnly: false
        {{- end }}
        - name: spire-server-socket
          mountPath: /tmp/spire-server/private
          readOnly: false
        livenessProbe:
          httpGet:
            path: /live
            port: http
          failureThreshold: 2
          initialDelaySeconds: 15
          periodSeconds: 60
          timeoutSeconds: 3
        readinessProbe:
          httpGet:
            path: /ready
            port: http
          initialDelaySeconds: 5
          periodSeconds: 5
        {{- with .Values.encryption.ztunnel.spire.install.server.securityContext }}
        securityContext:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      {{- with .Values.encryption.ztunnel.spire.install.server.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.encryption.ztunnel.spire.install.server.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.encryption.ztunnel.spire.install.server.tolerations }}
      tolerations:
        {{- toYaml . | trim | nindent 8 }}
      {{- end }}
      volumes:
      - name: spire-config
        configMap:
          name: spire-server
      - name: spire-server-socket
        hostPath:
          path: /var/run/spire-server/sockets
          type: DirectoryOrCreate
  {{- if .Values.encryption.ztunnel.spire.install.server.dataStorage.enabled }}
  volumeClaimTemplates:
  - metadata:
      name: spire-data
    spec:
      accessModes:
      - {{ .Values.encryption.ztunnel.spire.install.server.dataStorage.accessMode | default "ReadWriteOnce" }}
      resources:
        requests:
          storage: {{ .Values.encryption.ztunnel.spire.install.server.dataStorage.size }}
      storageClassName: {{ .Values.encryption.ztunnel.spire.install.server.dataStorage.storageClass }}
  {{- end }}
{{- end }}
