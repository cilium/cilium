{{- if and .Values.agent (not .Values.preflight.enabled) }}
---
apiVersion: admissionregistration.k8s.io/v1alpha1
kind: ValidatingAdmissionPolicyBinding
metadata:
  name: "http-tcp-binding"
spec:
  policyName: "http-tcp"
  matchResources:
{{- end }}
