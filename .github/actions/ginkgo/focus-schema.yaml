focus: list(str())
include: list(include('includeItem'))
exclude: list(include('excludeItem'))
---
includeItem:
  focus: str()
  cliFocus: str()
  cliSkip: str(required=False)
excludeItem:
  k8s-version: str()
  focus: str()
