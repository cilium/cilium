name: maintainers-little-helper-v1.5

on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - opened
      - edited
      - closed
      - reopened
      - synchronize
      - ready_for_review
      - locked
      - unlocked
    branches:
      - v1.5

jobs:
  cilium-github-actions:
    runs-on: ubuntu-latest
    steps:
      - name: Labeler
        uses: docker://docker.io/cilium/github-actions:v0.1.0
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          CONFIG_PATH: .github/cilium-actions-v1.5.yml
