name: Conformance Ztunnel (ci-ztunnel)

# Any change in triggers needs to be reflected in the concurrency group.
on:
  pull_request: {}
  workflow_dispatch:
    inputs:
      PR-number:
        description: "Pull request number."
        required: true
      context-ref:
        description: "Context in which the workflow runs. If PR is from a fork, will be the PR target branch (general case). If PR is NOT from a fork, will be the PR branch itself (this allows committers to test changes to workflows directly from PRs)."
        required: true
      SHA:
        description: "SHA under test (head of the PR branch)."
        required: true
      base-SHA:
        description: "SHA of the base branch (target branch of the PR)."
        required: false
      extra-args:
        description: "[JSON object] Arbitrary arguments passed from the trigger comment via regex capture group. Parse with 'fromJson(inputs.extra-args).argName' in workflow."
        required: false
        default: '{}'
  # Run every 12 hours
  schedule:
    - cron: '0 4/12 * * *'

# By specifying the access of one of the scopes, all of those that are not
# specified are set to 'none'.
permissions:
  # To be able to trigger eks-cluster-pool-manager.yaml workflow
  actions: write
  # To be able to access the repository with actions/checkout
  contents: read
  # To allow retrieving information from the PR API
  pull-requests: read
  # To be able to set commit status
  statuses: write
  # To be able to request the JWT from GitHub's OIDC provider
  id-token: write

concurrency:
  # Structure:
  # - Parent concurrency group name to avoid deadlock with child workflows
  # - Workflow name
  # - Event type
  # - A unique identifier depending on event type:
  #   - pull_request: PR number
  #   - schedule: SHA
  #   - workflow_dispatch: PR number
  #
  # This structure ensures a unique concurrency group name is generated for each
  # type of testing, such that re-runs will cancel the previous run.
  group: |
    parent
    ${{ github.workflow }}
    ${{ github.event_name }}
    ${{
      (github.event_name == 'pull_request' && github.event.pull_request.number) ||
      (github.event_name == 'push' && github.sha) ||
      (github.event_name == 'schedule' && github.sha) ||
      (github.event_name == 'workflow_dispatch' && github.event.inputs.PR-number)
    }}
  cancel-in-progress: true

jobs:
  echo-inputs:
    if: ${{ github.event_name == 'workflow_dispatch' }}
    name: Echo Workflow Dispatch Inputs
    runs-on: ubuntu-24.04
    steps:
      - name: Echo Workflow Dispatch Inputs
        run: |
          echo '${{ tojson(inputs) }}'

  commit-status-start:
    name: Commit Status Start
    runs-on: ubuntu-24.04
    steps:
      - name: Set initial commit status
        uses: myrotvorets/set-commit-status-action@3730c0a348a2ace3c110851bed53331bc6406e9f # v2.0.1
        with:
          sha: ${{ inputs.SHA || github.event.pull_request.head.sha || github.sha }}

  wait-for-images:
    name: Wait for images
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout context ref (trusted)
        uses: actions/checkout@8e8c483db84b4bee98b60c0593521ed34d9990e8 # v6.0.1
        with:
          ref: ${{ inputs.context-ref || github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false

      - name: Wait for images
        uses: ./.github/actions/wait-for-images
        with:
          SHA: ${{ inputs.SHA || github.event.pull_request.head.sha || github.sha }}
          images: cilium-ci operator-generic-ci hubble-relay-ci

  conformance-aks-ztunnel:
    name: Conformance AKS with Ztunnel
    needs: wait-for-images
    uses: ./.github/workflows/conformance-aks.yaml
    secrets: inherit
    with:
      PR-number: ${{ inputs.PR-number || github.event.pull_request.number || github.ref_name }}
      context-ref: ${{ inputs.context-ref || github.event.pull_request.head.sha || github.sha }}
      SHA: ${{ inputs.SHA || github.event.pull_request.head.sha || github.sha }}
      extra-args: '{"ztunnel": true}'

  conformance-eks-ztunnel:
    name: Conformance EKS with Ztunnel
    needs: wait-for-images
    uses: ./.github/workflows/conformance-eks.yaml
    secrets: inherit
    with:
      PR-number: ${{ inputs.PR-number || github.event.pull_request.number || github.ref_name }}
      context-ref: ${{ inputs.context-ref || github.event.pull_request.head.sha || github.sha }}
      SHA: ${{ inputs.SHA || github.event.pull_request.head.sha || github.sha }}
      extra-args: '{"ztunnel": true}'

  conformance-gke-ztunnel:
    name: Conformance GKE with Ztunnel
    needs: wait-for-images
    uses: ./.github/workflows/conformance-gke.yaml
    secrets: inherit
    with:
      PR-number: ${{ inputs.PR-number || github.event.pull_request.number || github.ref_name }}
      context-ref: ${{ inputs.context-ref || github.event.pull_request.head.sha || github.sha }}
      SHA: ${{ inputs.SHA || github.event.pull_request.head.sha || github.sha }}
      extra-args: '{"ztunnel": true}'

  merge-upload-and-status:
    name: Merge Upload and Status
    if: ${{ always() }}
    needs: [conformance-aks-ztunnel, conformance-eks-ztunnel, conformance-gke-ztunnel]
    uses: ./.github/workflows/common-post-jobs.yaml
    secrets: inherit
    with:
      context-ref: ${{ inputs.context-ref || github.event.pull_request.head.sha || github.sha }}
      sha: ${{ inputs.SHA || github.event.pull_request.head.sha || github.sha }}
      success: ${{ (needs.conformance-aks-ztunnel.result == 'success' && needs.conformance-eks-ztunnel.result == 'success' && needs.conformance-gke-ztunnel.result == 'success') }}
