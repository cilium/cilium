name: empty

on:
  pull_request:
    branches:
      - hackathon
jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - name: check
        run: |
          echo "hello"