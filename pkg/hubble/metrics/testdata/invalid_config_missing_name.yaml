metrics:
- name:
  contextOptions:
  - name: "labelsContext"
    values: ["source_namespace", "source_pod", "destination_pod"]
- name:
  contextOptions:
  - name: "destinationContext"
    values: ["dns", "ip"]
