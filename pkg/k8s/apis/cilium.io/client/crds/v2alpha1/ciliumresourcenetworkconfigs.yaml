---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: ciliumresourcenetworkconfigs.cilium.io
spec:
  group: cilium.io
  names:
    categories:
    - cilium
    kind: CiliumResourceNetworkConfig
    listKind: CiliumResourceNetworkConfigList
    plural: ciliumresourcenetworkconfigs
    shortNames:
    - crnc
    singular: ciliumresourcenetworkconfig
  scope: Cluster
  versions:
  - name: v2alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CiliumResourceNetworkConfig defines the network parameters to configure a network resource
          claimed from a workload.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            items:
              properties:
                ipPool:
                  description: IPPool is the IP pool from which to get addresses
                  type: string
                ipv4:
                  description: IPv4 specifies the network configuration for allocated
                    IPv4 addresses
                  properties:
                    netMask:
                      description: Netmask is the network mask associated to the allocated
                        IPv4 addresses
                      maximum: 32
                      minimum: 1
                      type: integer
                    staticRoutes:
                      items:
                        properties:
                          destination:
                            description: Destination specifies the route destination
                              parameter
                            format: cidr
                            type: string
                          gateway:
                            description: Gateway specifies the route gateway address
                            format: ipv4
                            type: string
                        required:
                        - destination
                        - gateway
                        type: object
                      type: array
                  required:
                  - netMask
                  type: object
                ipv6:
                  description: IPv6 specifies the network configuration for allocated
                    IPv6 addresses
                  properties:
                    netMask:
                      description: Netmask is the network mask associated to the allocated
                        IPv6 addresses
                      maximum: 128
                      minimum: 1
                      type: integer
                    staticRoutes:
                      description: StaticRoutes lists the routes that are added when
                        configuring the network resource
                      items:
                        properties:
                          destination:
                            description: Destination specifies the route destination
                              parameter
                            format: cidr
                            type: string
                          gateway:
                            description: Gateway specifies the route gateway address
                            format: ipv6
                            type: string
                        required:
                        - destination
                        - gateway
                        type: object
                      type: array
                  required:
                  - netMask
                  type: object
                nodeSelector:
                  description: |-
                    NodeSelector selects a group of nodes where this configuration
                    should be applied
                    If empty / nil this config applies to all nodes.
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: |-
                          A label selector requirement is a selector that contains values, a key, and an operator that
                          relates the key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: |-
                              operator represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists and DoesNotExist.
                            enum:
                            - In
                            - NotIn
                            - Exists
                            - DoesNotExist
                            type: string
                          values:
                            description: |-
                              values is an array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    matchLabels:
                      additionalProperties:
                        description: MatchLabelsValue represents the value from the
                          MatchLabels {key,value} pair.
                        maxLength: 63
                        pattern: ^(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])?$
                        type: string
                      description: |-
                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                      type: object
                  type: object
                  x-kubernetes-map-type: atomic
                vlan:
                  description: |-
                    VLAN is the VLAN ID to configure on devices using this network config.
                    A value of 0 means no VLAN is configured.
                  maximum: 4094
                  minimum: 0
                  type: integer
              type: object
            minItems: 1
            type: array
        required:
        - spec
        type: object
    served: true
    storage: true
