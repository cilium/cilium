---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: ciliumgatewayl4configs.cilium.io
spec:
  group: cilium.io
  names:
    categories:
    - cilium
    kind: CiliumGatewayL4Config
    listKind: CiliumGatewayL4ConfigList
    plural: ciliumgatewayl4configs
    shortNames:
    - cgl4
    singular: ciliumgatewayl4config
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.gatewayRef.name
      name: Gateway
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v2alpha1
    schema:
      openAPIV3Schema:
        description: |-
          CiliumGatewayL4Config is a Kubernetes third-party resource which carries
          L4 Gateway listener frontends and weighted backends for LB map programming.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Spec defines desired state for L4 Gateway listeners.
            properties:
              gatewayRef:
                description: GatewayRef references the Gateway that produced this
                  config.
                properties:
                  name:
                    description: Name is the Gateway name.
                    minLength: 1
                    type: string
                  namespace:
                    description: |-
                      Namespace is the Gateway namespace. If omitted, the namespace of the
                      CiliumGatewayL4Config is used.
                    type: string
                required:
                - name
                type: object
              listeners:
                description: Listeners defines L4 listeners with ports and weighted
                  backends.
                items:
                  description: CiliumGatewayL4Listener defines a single L4 listener
                    and its backends.
                  properties:
                    addresses:
                      description: Addresses are IPs assigned to the Gateway for this
                        listener.
                      items:
                        type: string
                      type: array
                      x-kubernetes-list-type: atomic
                    backends:
                      description: Backends is the list of weighted Service backends.
                      items:
                        description: CiliumGatewayL4Backend is a weighted backend
                          Service.
                        properties:
                          name:
                            description: Name is the Service name.
                            minLength: 1
                            type: string
                          namespace:
                            description: Namespace is the Service namespace. If omitted,
                              the Gateway namespace is used.
                            type: string
                          port:
                            description: Port is the Service port number.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                          weight:
                            default: 1
                            description: Weight defines the relative load-balancing
                              weight.
                            format: int32
                            maximum: 1000000
                            minimum: 0
                            type: integer
                        required:
                        - name
                        - port
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    name:
                      description: Name is the Gateway listener name.
                      minLength: 1
                      type: string
                    port:
                      description: Port is the listener port.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    protocol:
                      description: Protocol is the listener protocol.
                      enum:
                      - TCP
                      - UDP
                      type: string
                  required:
                  - name
                  - port
                  - protocol
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - gatewayRef
            type: object
          status:
            description: Status is the status of the configuration.
            properties:
              conditions:
                description: Current status conditions for the config.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        required:
        - metadata
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
