PREFIX?=/usr
BINDIR?=$(PREFIX)/bin
RUNDIR?=/var/run
CONFDIR?=/etc

INSTALL = install

VERSION = $(shell cat $(dir $(lastword $(MAKEFILE_LIST)))/VERSION)

# Uncomment to enable race detection
#GOBUILD = -race
